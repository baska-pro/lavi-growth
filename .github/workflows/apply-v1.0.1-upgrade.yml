name: Apply v1.0.1 Reliability Migration

on:
  push:
    branches: [fix/security-data-v1.0.1]

permissions:
  contents: write

jobs:
  migrate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/security-data-v1.0.1
          fetch-depth: 0

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Apply source migration
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re

          def read(path):
              return Path(path).read_text()

          def write(path, text):
              Path(path).write_text(text)

          def replace_once(text, old, new, label):
              count = text.count(old)
              if count != 1:
                  raise RuntimeError(f'{label}: expected exactly 1 match, found {count}')
              return text.replace(old, new, 1)

          # ------------------------------------------------------------------
          # utils.ts: migrate app state to IndexedDB + versioned backup format.
          # ------------------------------------------------------------------
          p = 'utils.ts'
          text = read(p)
          text = replace_once(
              text,
              "import { WHO_WEIGHT_BOYS, WHO_WEIGHT_GIRLS } from './data/medicalData';",
              "import { WHO_WEIGHT_BOYS, WHO_WEIGHT_GIRLS } from './data/medicalData';\nimport { clearAppStateDb, clearQueueDb, loadAppStateFromDb, saveAppStateToDb } from './services/localDb';",
              'utils localDb import'
          )
          text = re.sub(
              r"const STORAGE_KEY = 'FAMHEALTH_DATA_V1';\n\n// --- ID Generator \(Simple & Short\) ---\nexport const generateId = \(\): string => \{\n  return Math\.random\(\)\.toString\(36\)\.substring\(2, 10\)\.toUpperCase\(\);\n\};\n\n// --- Local Storage Helpers ---.*?// --- DYNAMIC THEME HELPER",
              """const LEGACY_STORAGE_KEY = 'FAMHEALTH_DATA_V1';

// --- ID Generator ---
export const generateId = (): string => {
  if (typeof crypto !== 'undefined' && typeof crypto.randomUUID === 'function') {
    return crypto.randomUUID().replace(/-/g, '').slice(0, 12).toUpperCase();
  }
  return `${Date.now().toString(36)}${Math.random().toString(36).slice(2, 8)}`.toUpperCase();
};

// --- IndexedDB State Helpers ---
export const loadStateAsync = async (): Promise<AppState | null> => {
  try {
    const fromDb = await loadAppStateFromDb();
    if (fromDb) return fromDb;
  } catch (error) {
    console.warn('IndexedDB load failed, checking legacy storage', error);
  }

  try {
    const serialized = localStorage.getItem(LEGACY_STORAGE_KEY);
    if (!serialized) return null;
    const legacy = JSON.parse(serialized) as AppState;
    try {
      await saveAppStateToDb(legacy);
      localStorage.removeItem(LEGACY_STORAGE_KEY);
    } catch (error) {
      console.warn('Legacy state migration to IndexedDB failed', error);
    }
    return legacy;
  } catch (error) {
    console.error('Failed to load legacy state', error);
    return null;
  }
};

export const saveStateAsync = async (state: AppState): Promise<void> => {
  try {
    await saveAppStateToDb(state);
    localStorage.removeItem(LEGACY_STORAGE_KEY);
  } catch (error) {
    console.error('IndexedDB save failed', error);
  }
};

// Compatibility wrappers for older call sites. New code should use async helpers.
export const loadState = (): AppState | null => {
  try {
    const serialized = localStorage.getItem(LEGACY_STORAGE_KEY);
    return serialized ? JSON.parse(serialized) : null;
  } catch {
    return null;
  }
};

export const saveState = (state: AppState): void => {
  void saveStateAsync(state);
};

export const clearLocalState = async (): Promise<void> => {
  localStorage.removeItem(LEGACY_STORAGE_KEY);
  localStorage.removeItem('LAVI_REMINDER_FIRED');
  await Promise.allSettled([clearAppStateDb(), clearQueueDb()]);
};

// --- DYNAMIC THEME HELPER""",
              text,
              count=1,
              flags=re.S
          )
          if 'loadStateAsync' not in text:
              raise RuntimeError('utils storage migration did not apply')

          text, n = re.subn(
              r"export const exportData = \(state: AppState\) => \{.*?\n\};\n\nexport const exportToExcel",
              """export const exportData = (state: AppState) => {
  const { pin, dbConfig, ...safeState } = state;
  const backup = {
    format: 'lavi-growth-backup',
    version: 2,
    exportedAt: new Date().toISOString(),
    data: safeState
  };
  const jsonString = JSON.stringify(backup, null, 2);
  const blob = new Blob([jsonString], { type: 'application/json' });
  const url = URL.createObjectURL(blob);
  const anchor = document.createElement('a');
  anchor.href = url;
  anchor.download = `LaviGrowth-Backup-${new Date().toISOString().split('T')[0]}.json`;
  document.body.appendChild(anchor);
  anchor.click();
  anchor.remove();
  URL.revokeObjectURL(url);
};

export const exportToExcel""",
              text,
              count=1,
              flags=re.S
          )
          if n != 1:
              raise RuntimeError(f'utils exportData replacement count={n}')
          write(p, text)

          # ------------------------------------------------------------------
          # App.tsx: reliable sync, restore, reminder, cascade deletion.
          # ------------------------------------------------------------------
          p = 'App.tsx'
          text = read(p)
          text = replace_once(
              text,
              "import { loadState, saveState, exportData, calculateDetailedAge, formatDate, savePinRecovery, exportToExcel, generatePDF, generateId, calculateAge } from './utils';",
              "import { loadStateAsync, saveStateAsync, clearLocalState, exportData, calculateDetailedAge, formatDate, savePinRecovery, exportToExcel, generatePDF, generateId, calculateAge } from './utils';",
              'App utils import'
          )
          text = replace_once(
              text,
              "import { pullFromCloud, pushToCloud, deleteFromCloud, checkPinOnServer, updatePinOnServer, resetServerData, processQueueFIFO, getQueueLength } from './services/api';",
              "import { pullFromCloud, pushToCloud, deleteFromCloud, checkPinOnServer, updatePinOnServer, resetServerData, processQueueFIFO, getQueueStatus, retryFailedQueue } from './services/api';",
              'App api import'
          )
          text = replace_once(
              text,
              "  const [pendingItems, setPendingItems] = useState(0);",
              "  const [pendingItems, setPendingItems] = useState(0);\n  const [failedItems, setFailedItems] = useState(0);",
              'App failed queue state'
          )

          reminder_pattern = r"  // --- NOTIFICATION LOGIC ---.*?\n  // --- PREGNANCY MODE ---"
          reminder_replacement = r'''  // --- NOTIFICATION LOGIC ---
  useEffect(() => {
      let intervalId: ReturnType<typeof setInterval> | null = null;
      const firedStorageKey = 'LAVI_REMINDER_FIRED';

      const localDateKey = (date: Date) => {
          const y = date.getFullYear();
          const m = String(date.getMonth() + 1).padStart(2, '0');
          const d = String(date.getDate()).padStart(2, '0');
          return `${y}-${m}-${d}`;
      };

      const alreadyFired = (key: string) => {
          try {
              const saved = JSON.parse(localStorage.getItem(firedStorageKey) || '{}');
              const cutoff = Date.now() - (48 * 60 * 60 * 1000);
              Object.keys(saved).forEach(k => { if (saved[k] < cutoff) delete saved[k]; });
              if (saved[key]) return true;
              saved[key] = Date.now();
              localStorage.setItem(firedStorageKey, JSON.stringify(saved));
              return false;
          } catch {
              return false;
          }
      };

      const notify = async (r: Reminder) => {
          const title = `Pengingat: ${r.title}`;
          const body = `Waktunya untuk ${r.title}.`;
          if (Notification.permission === 'granted') {
              try {
                  if ('serviceWorker' in navigator) {
                      const registration = await navigator.serviceWorker.ready;
                      await registration.showNotification(title, {
                          body,
                          icon: '/favicon.svg',
                          badge: '/favicon.svg',
                          tag: `lavi-reminder-${r.id}`
                      });
                      return;
                  }
                  new Notification(title, { body, icon: '/favicon.svg' });
                  return;
              } catch (error) {
                  console.warn('Notification delivery failed', error);
              }
          }
          showToast(`Waktunya: ${r.title}`, 'info');
      };

      const checkReminders = () => {
          if (!state.reminders?.length) return;
          const now = new Date();
          const currentDay = now.getDay();
          const currentTime = `${String(now.getHours()).padStart(2, '0')}:${String(now.getMinutes()).padStart(2, '0')}`;
          const today = localDateKey(now);

          state.reminders.forEach(r => {
              if (!r.active || r.time !== currentTime) return;
              const matchesDate = r.specificDate ? r.specificDate === today : r.days.includes(currentDay);
              if (!matchesDate) return;
              const fireKey = `${r.id}:${today}:${currentTime}`;
              if (!alreadyFired(fireKey)) void notify(r);
          });
      };

      checkReminders();
      const now = new Date();
      const delay = Math.max(250, (60 - now.getSeconds()) * 1000 - now.getMilliseconds());
      const timeoutId = setTimeout(() => {
          checkReminders();
          intervalId = setInterval(checkReminders, 60_000);
      }, delay);

      return () => {
          clearTimeout(timeoutId);
          if (intervalId) clearInterval(intervalId);
      };
  }, [state.reminders]);

  // --- PREGNANCY MODE ---'''
          text, n = re.subn(reminder_pattern, reminder_replacement, text, count=1, flags=re.S)
          if n != 1:
              raise RuntimeError(f'App reminder replacement count={n}')

          text = replace_once(
              text,
              "        await processQueueFIFO();\n        setPendingItems(getQueueLength());\n        const cloudState = await pullFromCloud(pin);",
              "        await processQueueFIFO();\n        const queueStatus = await getQueueStatus();\n        setPendingItems(queueStatus.pending);\n        setFailedItems(queueStatus.failed);\n        // Never overwrite local changes while anything is still pending or failed.\n        if (queueStatus.total > 0) return;\n        const cloudState = await pullFromCloud(pin);",
              'App pull queue guard'
          )

          text = replace_once(
              text,
              "    setPendingItems(getQueueLength());",
              "    void getQueueStatus().then(q => { setPendingItems(q.pending); setFailedItems(q.failed); });",
              'App initial queue status'
          )
          text = replace_once(text, "    }, 10000);", "    }, 180000);", 'App sync interval')

          init_pattern = r"  useEffect\(\(\) => \{\n    const loaded = loadState\(\);.*?\n    setIsLoaded\(true\);\n  \}, \[\]\);"
          init_replacement = r'''  useEffect(() => {
    let cancelled = false;
    void (async () => {
      const loaded = await loadStateAsync();
      if (cancelled) return;
      if (loaded) {
        setState(prev => ({
          ...prev,
          ...loaded,
          profiles: Array.isArray(loaded.profiles) ? loaded.profiles : [],
          records: Array.isArray(loaded.records) ? loaded.records : [],
          targets: Array.isArray(loaded.targets) ? loaded.targets : [],
          vaccines: Array.isArray(loaded.vaccines) ? loaded.vaccines : [],
          milestones: Array.isArray(loaded.milestones) ? loaded.milestones : [],
          reminders: Array.isArray(loaded.reminders) ? loaded.reminders : [],
          menstrualCycles: Array.isArray(loaded.menstrualCycles) ? loaded.menstrualCycles : []
        }));
        if (loaded.pin && !loaded.isOfflineMode) setTimeout(() => performPullSync(loaded.pin), 1000);
        if (loaded.profiles?.length) setTimeout(() => setShowDailyBriefing(true), 1500);
      }
      const q = await getQueueStatus();
      if (!cancelled) {
        setPendingItems(q.pending);
        setFailedItems(q.failed);
        setIsLoaded(true);
      }
    })();
    return () => { cancelled = true; };
  }, []);'''
          text, n = re.subn(init_pattern, init_replacement, text, count=1, flags=re.S)
          if n != 1:
              raise RuntimeError(f'App initial load replacement count={n}')

          text = replace_once(
              text,
              "  useEffect(() => { if (isLoaded) { saveState(state); if (state.darkMode) document.documentElement.classList.add('dark'); else document.documentElement.classList.remove('dark'); } }, [state, isLoaded]);",
              "  useEffect(() => { if (isLoaded) { void saveStateAsync(state); if (state.darkMode) document.documentElement.classList.add('dark'); else document.documentElement.classList.remove('dark'); } }, [state, isLoaded]);",
              'App async save'
          )

          text = replace_once(
              text,
              "  const resetProfileForm = () => {",
              """  const handleRetryFailedSync = async () => {
      if (!navigator.onLine) return showToast('Anda sedang offline', 'warning');
      setIsSyncing(true);
      try {
          const retried = await retryFailedQueue();
          await processQueueFIFO();
          const q = await getQueueStatus();
          setPendingItems(q.pending);
          setFailedItems(q.failed);
          if (q.total === 0 && state.pin && !state.isOfflineMode) await performPullSync(state.pin);
          showToast(retried > 0 ? 'Antrian gagal dicoba ulang.' : 'Tidak ada antrian gagal.', 'info');
      } finally {
          setIsSyncing(false);
      }
  };
  const resetProfileForm = () => {""",
              'App retry handler'
          )

          old_delete = "const newState = { ...state, profiles: newProfiles, records: state.records.filter(r => r.profileId !== targetId), activeProfileId: newProfiles[0]?.id || null };"
          new_delete = "const newState = { ...state, profiles: newProfiles, records: state.records.filter(r => r.profileId !== targetId), vaccines: (state.vaccines || []).filter(v => v.profileId !== targetId), milestones: (state.milestones || []).filter(m => m.profileId !== targetId), targets: (state.targets || []).filter(t => t.profileId !== targetId), reminders: (state.reminders || []).filter(r => r.profileId !== targetId), menstrualCycles: (state.menstrualCycles || []).filter(c => c.profileId !== targetId), activeProfileId: newProfiles[0]?.id || null };"
          text = replace_once(text, old_delete, new_delete, 'App profile cascade delete')

          file_pattern = r"  const handleFileSelect = \(e: React\.ChangeEvent<HTMLInputElement>\) => \{.*?\n  const handleExport = \(\) =>"
          file_replacement = r'''  const handleFileSelect = (e: React.ChangeEvent<HTMLInputElement>) => {
      const file = e.target.files?.[0];
      e.target.value = '';
      if (!file) return;
      if (file.size > 25 * 1024 * 1024) return showToast('File backup terlalu besar (maks. 25 MB).', 'error');

      const fileReader = new FileReader();
      fileReader.readAsText(file, 'UTF-8');
      fileReader.onload = event => {
          try {
              const raw = JSON.parse(event.target?.result as string);
              const parsed = raw?.format === 'lavi-growth-backup' ? raw.data : raw;
              if (!parsed || !Array.isArray(parsed.profiles) || !Array.isArray(parsed.records)) {
                  return showToast('Format backup tidak valid.', 'error');
              }
              setImportPreview({
                  profiles: parsed.profiles,
                  records: parsed.records,
                  vaccines: Array.isArray(parsed.vaccines) ? parsed.vaccines : [],
                  milestones: Array.isArray(parsed.milestones) ? parsed.milestones : [],
                  targets: Array.isArray(parsed.targets) ? parsed.targets : [],
                  reminders: Array.isArray(parsed.reminders) ? parsed.reminders : [],
                  menstrualCycles: Array.isArray(parsed.menstrualCycles) ? parsed.menstrualCycles : [],
                  activeProfileId: parsed.activeProfileId || parsed.profiles[0]?.id || null,
                  darkMode: typeof parsed.darkMode === 'boolean' ? parsed.darkMode : state.darkMode
              });
          } catch {
              showToast('File rusak atau tidak valid.', 'error');
          }
      };
  };
  const handleExport = () =>'''
          text, n = re.subn(file_pattern, file_replacement, text, count=1, flags=re.S)
          if n != 1:
              raise RuntimeError(f'App import parser replacement count={n}')

          import_pattern = r"  const confirmImport = async \(\) => \{.*?\n  const handleDirectLogin = async \(\) =>"
          import_replacement = r'''  const confirmImport = async () => {
      if (!importPreview) return;
      setGlobalLoading('Mengimpor & Memvalidasi Data...');
      const importedState: AppState = {
          ...initialState,
          profiles: importPreview.profiles,
          records: importPreview.records,
          vaccines: importPreview.vaccines,
          milestones: importPreview.milestones,
          targets: importPreview.targets,
          reminders: importPreview.reminders,
          menstrualCycles: importPreview.menstrualCycles,
          activeProfileId: importPreview.activeProfileId,
          darkMode: importPreview.darkMode,
          pin: state.pin,
          isOfflineMode: state.isOfflineMode
      };
      setState(importedState);
      await saveStateAsync(importedState);

      if (state.pin && !state.isOfflineMode) {
          await pushToCloud({
              profiles: importedState.profiles,
              records: importedState.records,
              vaccines: importedState.vaccines,
              milestones: importedState.milestones,
              targets: importedState.targets,
              reminders: importedState.reminders,
              menstrualCycles: importedState.menstrualCycles
          }, state.pin);
          const q = await getQueueStatus();
          setPendingItems(q.pending);
          setFailedItems(q.failed);
          showToast(q.total === 0 ? 'Data berhasil diimpor dan tersinkron.' : 'Data berhasil diimpor. Sinkronisasi masuk antrian.', q.failed ? 'warning' : 'success');
      } else {
          showToast('Data berhasil diimpor ke penyimpanan lokal.', 'success');
      }
      setGlobalLoading(null);
      setImportPreview(null);
  };
  const handleDirectLogin = async () =>'''
          text, n = re.subn(import_pattern, import_replacement, text, count=1, flags=re.S)
          if n != 1:
              raise RuntimeError(f'App confirmImport replacement count={n}')

          login_pattern = r"  const handleDirectLogin = async \(\) => \{.*?\n  const handlePinUpdate = async"
          login_replacement = r'''  const handleDirectLogin = async () => {
      if (!/^\d{4,12}$/.test(pinInput)) return showToast('PIN harus berupa 4-12 angka.', 'error');
      setGlobalLoading('Verifikasi & Mengunduh Data...');
      const loginPin = pinInput;
      const isValid = await checkPinOnServer(loginPin);
      if (isValid) {
          const cloudState = await pullFromCloud(loginPin);
          if (cloudState) {
              const fixRecords = (recs: any[]) => recs.map(r => {
                  if (!r.timestamp) {
                      const t = r.time || '23:59';
                      return { ...r, timestamp: new Date(`${r.date}T${t}`).getTime() };
                  }
                  return r;
              });
              const nextState: AppState = {
                  ...state,
                  pin: loginPin,
                  profiles: cloudState.profiles,
                  records: fixRecords(cloudState.records),
                  vaccines: cloudState.vaccines,
                  milestones: cloudState.milestones,
                  targets: cloudState.targets || [],
                  reminders: cloudState.reminders || [],
                  menstrualCycles: cloudState.menstrualCycles || [],
                  activeProfileId: cloudState.profiles[0]?.id || null,
                  lastSyncTime: Date.now()
              };
              setState(nextState);
              await saveStateAsync(nextState);
              setShowLoginModal(false);
              setPinInput('');
              showToast('Berhasil Masuk & Data Tersinkron!', 'success');
          } else {
              showToast('PIN benar tetapi data cloud gagal diambil.', 'warning');
          }
      } else {
          showToast('PIN salah atau akses sedang dibatasi sementara.', 'error');
      }
      setGlobalLoading(null);
  };
  const handlePinUpdate = async'''
          text, n = re.subn(login_pattern, login_replacement, text, count=1, flags=re.S)
          if n != 1:
              raise RuntimeError(f'App direct login replacement count={n}')

          text = replace_once(
              text,
              'if (pinInput === state.pin || pinInput === "0000") isValid = true;',
              'if (pinInput === state.pin) isValid = true;',
              'App remove reset bypass'
          )
          reset_pattern = r"  const executeHardReset = async \(\) => \{.*?\n\n  if \(!isLoaded\) return null;"
          reset_replacement = r'''  const executeHardReset = async () => {
      setGlobalLoading('Mereset Aplikasi...');
      await clearLocalState();
      setState(initialState);
      setGlobalLoading(null);
      setShowResetConfirmModal(false);
      showToast('Data lokal aplikasi direset.', 'success');
      setTimeout(() => window.location.reload(), 1000);
  };

  if (!isLoaded) return null;'''
          text, n = re.subn(reset_pattern, reset_replacement, text, count=1, flags=re.S)
          if n != 1:
              raise RuntimeError(f'App hard reset replacement count={n}')

          pending_ui = "{pendingItems > 0 && isOnline && !state.isOfflineMode && ( <div className=\"fixed bottom-20 left-1/2 -translate-x-1/2 z-[90] bg-blue-600 text-white px-4 py-2 rounded-full text-xs font-bold flex items-center gap-2 shadow-lg\"> <RefreshCw size={14} className=\"animate-spin\" /> Menyinkronkan {pendingItems} item... </div> )}"
          failed_ui = pending_ui + "\n      {failedItems > 0 && isOnline && !state.isOfflineMode && ( <button onClick={handleRetryFailedSync} className=\"fixed bottom-32 left-1/2 -translate-x-1/2 z-[91] bg-red-600 text-white px-4 py-2 rounded-full text-xs font-bold flex items-center gap-2 shadow-lg\"> <AlertTriangle size={14} /> {failedItems} sync gagal · Coba lagi </button> )}"
          text = replace_once(text, pending_ui, failed_ui, 'App failed queue UI')
          write(p, text)

          # ------------------------------------------------------------------
          # Reminder editor: preserve active state when editing.
          # ------------------------------------------------------------------
          p = 'components/features/Reminders.tsx'
          text = read(p)
          text = replace_once(
              text,
              "            active: true, // WAJIB TRUE SAAT BUAT BARU",
              "            active: editingId ? (reminders.find(r => r.id === editingId)?.active ?? true) : true,",
              'Reminder active state'
          )
          write(p, text)

          # ------------------------------------------------------------------
          # Settings: enforce strong numeric PIN for all new PINs.
          # ------------------------------------------------------------------
          p = 'components/features/Settings.tsx'
          text = read(p)
          text = replace_once(
              text,
              "    if (newPin.length < 4) {\n      if(onShowToast) onShowToast(\"PIN baru minimal 4 angka\", \"error\");\n      return;\n    }",
              "    if (!/^\\d{6,12}$/.test(newPin)) {\n      if(onShowToast) onShowToast(\"PIN baru harus 6-12 angka\", \"error\");\n      return;\n    }",
              'Settings PIN validation'
          )
          text = text.replace('placeholder="****"', 'placeholder="PIN lama" inputMode="numeric" autoComplete="current-password"', 1)
          text = text.replace('placeholder="****"', 'placeholder="6-12 angka" inputMode="numeric" autoComplete="new-password" maxLength={12}', 1)
          text = replace_once(
              text,
              '*PIN digunakan untuk sinkronisasi data antar perangkat.',
              '*PIN baru wajib 6-12 angka. Server menyimpan hash PIN, bukan PIN asli.',
              'Settings PIN note'
          )
          write(p, text)

          # ------------------------------------------------------------------
          # GAS backend: hash PIN, random initial PIN, rate-limit attempts.
          # Apply to standalone script and copyable TS template.
          # ------------------------------------------------------------------
          for p in ['ScriptGAS.gs', 'services/gasScriptContent.ts']:
              text = read(p)
              text = text.replace('LAVI GROWTH TRACKER - BACKEND V8.0 (Fix Date Timezone Bug)', 'LAVI GROWTH TRACKER - BACKEND V9.0 (Hashed PIN + Rate Limit)')
              text = replace_once(
                  text,
                  '  DEFAULT_PIN: "1234",',
                  '  PIN_MIN_LENGTH: 6,\n  MAX_FAILED_ATTEMPTS: 5,\n  LOCK_MINUTES: 5,',
                  f'{p} remove default PIN'
              )
              text = replace_once(
                  text,
                  "  if (!props.getProperty('APP_PIN')) props.setProperty('APP_PIN', CONFIG.DEFAULT_PIN);",
                  """  migrateLegacyPin(props);
  if (!props.getProperty('APP_PIN_HASH')) {
    const initialPin = String(Math.floor(100000 + Math.random() * 900000));
    setPinHash(initialPin, props);
    Logger.log('PIN awal Lavi Growth: ' + initialPin + ' (simpan lalu ganti dari aplikasi)');
  }""",
                  f'{p} initial PIN'
              )

              marker = "\nfunction initialSetup() {"
              helpers = r'''
function pinSalt(props) {
  let salt = props.getProperty('PIN_SALT');
  if (!salt) {
    salt = Utilities.getUuid() + Utilities.getUuid();
    props.setProperty('PIN_SALT', salt);
  }
  return salt;
}

function hashPin(pin, props) {
  const input = pinSalt(props) + ':' + String(pin);
  const bytes = Utilities.computeDigest(Utilities.DigestAlgorithm.SHA_256, input, Utilities.Charset.UTF_8);
  return bytes.map(function(b) { const v = b < 0 ? b + 256 : b; return ('0' + v.toString(16)).slice(-2); }).join('');
}

function setPinHash(pin, props) {
  props.setProperty('APP_PIN_HASH', hashPin(pin, props));
  props.deleteProperty('APP_PIN');
  clearPinFailures(props);
}

function verifyPin(pin, props) {
  const stored = props.getProperty('APP_PIN_HASH');
  return !!stored && stored === hashPin(pin, props);
}

function migrateLegacyPin(props) {
  const legacy = props.getProperty('APP_PIN');
  if (legacy && !props.getProperty('APP_PIN_HASH')) setPinHash(String(legacy), props);
}

function isPinLocked(props) {
  const until = Number(props.getProperty('PIN_LOCKED_UNTIL') || 0);
  return until > Date.now();
}

function clearPinFailures(props) {
  props.setProperty('PIN_FAILED_ATTEMPTS', '0');
  props.deleteProperty('PIN_LOCKED_UNTIL');
}

function recordPinFailure(props) {
  let attempts = Number(props.getProperty('PIN_FAILED_ATTEMPTS') || 0) + 1;
  if (attempts >= CONFIG.MAX_FAILED_ATTEMPTS) {
    props.setProperty('PIN_FAILED_ATTEMPTS', '0');
    props.setProperty('PIN_LOCKED_UNTIL', String(Date.now() + CONFIG.LOCK_MINUTES * 60 * 1000));
  } else {
    props.setProperty('PIN_FAILED_ATTEMPTS', String(attempts));
  }
}

function validNewPin(pin) {
  return new RegExp('^[0-9]{' + CONFIG.PIN_MIN_LENGTH + ',12}$').test(String(pin || ''));
}
'''
              text = replace_once(text, marker, helpers + marker, f'{p} PIN helpers')

              do_post_pattern = r"function doPost\(e\) \{.*?\n\}\n\nfunction handlePull\(\) \{"
              do_post_replacement = r'''function doPost(e) {
  const lock = LockService.getScriptLock();
  if (!lock.tryLock(5000)) return responseJSON({ status: 'error', message: 'Server busy' });

  try {
    const props = PropertiesService.getScriptProperties();
    migrateLegacyPin(props);
    if (!props.getProperty('APP_PIN_HASH')) initialSetup();

    const postData = JSON.parse(e.postData.contents || '{}');
    const action = postData.action;
    const clientPin = String(postData.pin || '');

    if (action === 'health_check') {
      return responseJSON({ status: 'success', version: '9.0' });
    }

    if (isPinLocked(props)) {
      return responseJSON({ status: 'error', message: 'Terlalu banyak percobaan PIN. Coba lagi beberapa menit.' });
    }

    const pinValid = verifyPin(clientPin, props);
    if (action === 'check_pin') {
      if (pinValid) clearPinFailures(props); else recordPinFailure(props);
      return responseJSON({ status: 'success', valid: pinValid });
    }

    if (!pinValid) {
      recordPinFailure(props);
      return responseJSON({ status: 'error', message: 'Invalid PIN' });
    }
    clearPinFailures(props);

    let result;
    switch (action) {
      case 'sync_pull': result = handlePull(); break;
      case 'sync_push': result = handlePush(postData.payload || {}); break;
      case 'delete_data': result = handleDelete(postData.type, postData.id); break;
      case 'upload_image': result = uploadImageToDrive(postData.fileData, postData.fileName); break;
      case 'update_pin':
        if (!validNewPin(postData.newPin)) result = { status: 'error', message: 'PIN baru harus 6-12 angka' };
        else { setPinHash(String(postData.newPin), props); result = { status: 'success' }; }
        break;
      case 'reset_data': result = resetAllData(); break;
      default: result = { status: 'error', message: 'Unknown Action' };
    }
    return responseJSON(result);
  } catch (err) {
    return responseJSON({ status: 'error', message: err.toString() });
  } finally {
    lock.releaseLock();
  }
}

function handlePull() {'''
              text, n = re.subn(do_post_pattern, do_post_replacement, text, count=1, flags=re.S)
              if n != 1:
                  raise RuntimeError(f'{p} doPost replacement count={n}')
              write(p, text)

          # ------------------------------------------------------------------
          # api.ts: clear queue during hard reset support.
          # ------------------------------------------------------------------
          p = 'services/api.ts'
          text = read(p)
          text = replace_once(
              text,
              "  StoredQueueItem,\n  deleteQueueItem,",
              "  StoredQueueItem,\n  clearQueueDb,\n  deleteQueueItem,",
              'api clear queue import'
          )
          text = replace_once(
              text,
              "export const getQueueLength = async (): Promise<number> => (await getQueueStatus()).total;",
              "export const getQueueLength = async (): Promise<number> => (await getQueueStatus()).total;\nexport const clearSyncQueue = async (): Promise<void> => { await clearQueueDb(); };",
              'api clear queue export'
          )
          write(p, text)

          # One-shot workflow removes itself from the resulting branch.
          Path('.github/workflows/apply-v1.0.1-upgrade.yml').unlink()
          PY

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Typecheck
        run: bun run typecheck

      - name: Build
        run: bun run build

      - name: Commit migrated sources
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: harden sync, reminders, backups and local storage"
          git push origin HEAD:fix/security-data-v1.0.1
